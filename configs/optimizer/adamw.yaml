# The engine completes this with the live model: instantiate(cfg.optimizer,
# params=model.parameters()) -> AdamW(params=..., **rest).
_target_: torch.optim.AdamW
lr: 5e-4
weight_decay: 0.05
betas: [0.9, 0.999]
eps: 1e-8
